<think-first>STOP. Before any planning tool:
1. Restate what the user is actually asking (not what's easy to do).
2. List your key assumptions. What's ambiguous?
3. What do you need to discover first? (Consider memory_search / read_file / search_content.)
4. Is this a small local change (→ TodoWrite) or a multi-step feature (→ task_create + DAG)?
Write your reasoning as plain text first, then call planning tools.</think-first>
